import DiceRoller from "./DiceRoller";
// Route-module boundary used by central route registry.
const DiceRollerRoute = {
  element: <DiceRoller />,
  path: "/dice",
};

export default DiceRollerRoute;
